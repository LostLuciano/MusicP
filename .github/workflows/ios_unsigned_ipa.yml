name: Build iOS IPA

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: # Memungkinkan trigger build secara manual dari tab Actions GitHub

jobs:
  build-ios:
    name: Build iOS App
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Clone Flutter SDK
        run: |
          git clone https://github.com/flutter/flutter.git -b stable --depth 1
          echo "$GITHUB_WORKSPACE/flutter/bin" >> $GITHUB_PATH

      - name: Install Dependencies
        run: |
          cd flutter_app
          flutter pub get

      - name: Build Release iOS App (No Codesign)
        run: |
          cd flutter_app
          flutter build ios --no-codesign --release

      - name: Package into IPA (Unsigned)
        run: |
          cd flutter_app
          mkdir -p Payload
          mv build/ios/iphoneos/Runner.app Payload/
          zip -r MusicStemStudio.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: music-stem-studio-ios-ipa
          path: flutter_app/MusicStemStudio.ipa
